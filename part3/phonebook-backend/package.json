{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Backend for Phonebook app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "render-build:ui": "rm -rf dist && cd ../phonebook-frontend && npm run build && cp -r dist ../phonebook-backend",
    "render-deploy:full": "npm run render-build:ui && git add . && git commit -m uibuild && git push"
  },
  "keywords": [
    "backend",
    "express",
    "node",
    "rest"
  ],
  "author": "Kiril Mishevski",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.21"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
